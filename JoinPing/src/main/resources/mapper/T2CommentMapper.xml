<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.joinping.mapper.T2CommentMapper">
    <select id="getByT1CommentId" parameterType="long" resultType="T2Comment">
        SELECT t1.*,
               u1.name as userName,
               u1.photoUrl,
               u2.name as replyUserName
        FROM joinping.t2comment t1
                 INNER JOIN joinping.user u1 ON t1.userId = u1.id
                 LEFT JOIN joinping.user u2 ON t1.replyUserId = u2.id
        WHERE t1.t1commentId = #{id}
          AND t1.status = 0
        ORDER BY t1.id
    </select>
</mapper>